diff --git a/dist/index.js b/dist/index.js
index 7008095442925300a84aac94f46ee206b802fd20..3be2258b5dfb9ea433afb1eed8a852ac36054770 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -860,6 +860,16 @@ var anthropicMessagesChunkSchema = (0, import_provider_utils2.lazySchema)(
     ])
   )
 );
+var anthropicMessagesChunkTypes = /* @__PURE__ */ new Set([
+  "message_start",
+  "content_block_start",
+  "content_block_delta",
+  "content_block_stop",
+  "message_delta",
+  "message_stop",
+  "ping",
+  "error"
+]);
 var anthropicReasoningMetadataSchema = (0, import_provider_utils2.lazySchema)(
   () => (0, import_provider_utils2.zodSchema)(
     import_v42.z.object({
@@ -1456,7 +1466,8 @@ async function prepareTools({
   cacheControlValidator,
   supportsStructuredOutput,
   supportsStrictTools,
-  defaultEagerInputStreaming = false
+  defaultEagerInputStreaming = false,
+  clewdr
 }) {
   var _a, _b;
   tools = (tools == null ? void 0 : tools.length) ? tools : void 0;
@@ -1488,7 +1499,7 @@ async function prepareTools({
         anthropicTools2.push({
           name: tool.name,
           description: tool.description,
-          input_schema: tool.inputSchema,
+          input_schema: clewdr ? sanitizeJsonSchema(tool.inputSchema) : tool.inputSchema,
           cache_control: cacheControl,
           ...eagerInputStreaming ? { eager_input_streaming: true } : {},
           ...supportsStrictTools === true && tool.strict != null ? { strict: tool.strict } : {},
@@ -2212,7 +2223,8 @@ async function convertToAnthropicMessagesPrompt({
   sendReasoning,
   warnings,
   cacheControlValidator,
-  toolNameMapping
+  toolNameMapping,
+  clewdr
 }) {
   var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u;
   const betas = /* @__PURE__ */ new Set();
@@ -2351,9 +2363,26 @@ async function convertToAnthropicMessagesPrompt({
                         cache_control: cacheControl
                       });
                     } else {
-                      throw new import_provider2.UnsupportedFunctionalityError({
-                        functionality: `media type: ${part.mediaType}`
-                      });
+                      const metadata = await getDocumentMetadata(
+                        part.providerOptions
+                      );
+                      if (clewdr) {
+                        anthropicContent.push({
+                          type: "document",
+                          source: {
+                            type: "base64",
+                            media_type: part.mediaType,
+                            data: (0, import_provider_utils2.convertToBase64)(part.data)
+                          },
+                          title: metadata.title != null ? metadata.title : part.filename != null ? part.filename : "file",
+                          ...metadata.context && { context: metadata.context },
+                          cache_control: cacheControl
+                        });
+                      } else {
+                        throw new import_provider2.UnsupportedFunctionalityError({
+                          functionality: `media type: ${part.mediaType}`
+                        });
+                      }
                     }
                     break;
                   }
@@ -3456,23 +3485,25 @@ var AnthropicMessagesLanguageModel = class {
         "anthropic.advisor_20260301": "advisor"
       }
     });
+    const clewdr = this.config.provider === "clewdr";
     const { prompt: messagesPrompt, betas } = await convertToAnthropicMessagesPrompt({
       prompt,
       sendReasoning: (_d = anthropicOptions == null ? void 0 : anthropicOptions.sendReasoning) != null ? _d : true,
       warnings,
       cacheControlValidator,
-      toolNameMapping
+      toolNameMapping,
+      clewdr
     });
     const thinkingType = (_e = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _e.type;
     const isThinking = thinkingType === "enabled" || thinkingType === "adaptive";
     let thinkingBudget = thinkingType === "enabled" ? (_f = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _f.budgetTokens : void 0;
     const thinkingDisplay = thinkingType === "adaptive" ? (_g = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _g.display : void 0;
-    const maxTokens = maxOutputTokens != null ? maxOutputTokens : maxOutputTokensForModel;
+    const maxTokens = maxOutputTokens != null ? maxOutputTokens : !clewdr || isKnownModel || this.modelId.startsWith("claude-") ? maxOutputTokensForModel : void 0;
     const baseArgs = {
       // model id:
       model: this.modelId,
       // standardized settings:
-      max_tokens: maxTokens,
+      ...clewdr && maxTokens == null ? {} : { max_tokens: maxTokens },
       temperature,
       top_k: topK,
       top_p: topP,
@@ -3485,6 +3516,9 @@ var AnthropicMessagesLanguageModel = class {
           ...thinkingDisplay != null && { display: thinkingDisplay }
         }
       },
+      ...clewdr && thinkingType === "disabled" && {
+        thinking: { type: "disabled" }
+      },
       ...((anthropicOptions == null ? void 0 : anthropicOptions.effort) || (anthropicOptions == null ? void 0 : anthropicOptions.taskBudget) || useStructuredOutput && (responseFormat == null ? void 0 : responseFormat.type) === "json" && responseFormat.schema != null) && {
         output_config: {
           ...(anthropicOptions == null ? void 0 : anthropicOptions.effort) && {
@@ -3643,7 +3677,9 @@ var AnthropicMessagesLanguageModel = class {
           details: "topP is not supported when thinking is enabled"
         });
       }
-      baseArgs.max_tokens = maxTokens + (thinkingBudget != null ? thinkingBudget : 0);
+      if (!clewdr || maxTokens != null) {
+        baseArgs.max_tokens = maxTokens + (thinkingBudget != null ? thinkingBudget : 0);
+      }
     } else {
       if (isAnthropicModel && topP != null && temperature != null) {
         warnings.push({
@@ -3709,7 +3745,8 @@ var AnthropicMessagesLanguageModel = class {
         cacheControlValidator,
         supportsStructuredOutput: false,
         supportsStrictTools,
-        defaultEagerInputStreaming
+        defaultEagerInputStreaming,
+        clewdr
       } : {
         tools: tools != null ? tools : [],
         toolChoice,
@@ -3717,7 +3754,8 @@ var AnthropicMessagesLanguageModel = class {
         cacheControlValidator,
         supportsStructuredOutput,
         supportsStrictTools,
-        defaultEagerInputStreaming
+        defaultEagerInputStreaming,
+        clewdr
       }
     );
     const cacheWarnings = cacheControlValidator.getWarnings();
@@ -4344,6 +4382,7 @@ var AnthropicMessagesLanguageModel = class {
     let isJsonResponseFromTool = false;
     let blockType = void 0;
     const generateId3 = this.generateId;
+    const clewdr = this.config.provider === "clewdr";
     const transformedStream = response.pipeThrough(
       new TransformStream({
         start(controller) {
@@ -4355,6 +4394,10 @@ var AnthropicMessagesLanguageModel = class {
             controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
           }
           if (!chunk.success) {
+            const rawType = typeof chunk.rawValue === "object" && chunk.rawValue != null ? chunk.rawValue.type : void 0;
+            if (clewdr && typeof rawType === "string" && !anthropicMessagesChunkTypes.has(rawType)) {
+              return;
+            }
             controller.enqueue({ type: "error", error: chunk.error });
             return;
           }
diff --git a/dist/index.mjs b/dist/index.mjs
index 073cc652eb10544e16823cd11d2f721fac1f771e..459d0000c6f5f8089f663a31bdc7f2a4505f88d9 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -858,6 +858,16 @@ var anthropicMessagesChunkSchema = lazySchema2(
     ])
   )
 );
+var anthropicMessagesChunkTypes = /* @__PURE__ */ new Set([
+  "message_start",
+  "content_block_start",
+  "content_block_delta",
+  "content_block_stop",
+  "message_delta",
+  "message_stop",
+  "ping",
+  "error"
+]);
 var anthropicReasoningMetadataSchema = lazySchema2(
   () => zodSchema2(
     z2.object({
@@ -1480,7 +1490,8 @@ async function prepareTools({
   cacheControlValidator,
   supportsStructuredOutput,
   supportsStrictTools,
-  defaultEagerInputStreaming = false
+  defaultEagerInputStreaming = false,
+  clewdr
 }) {
   var _a, _b;
   tools = (tools == null ? void 0 : tools.length) ? tools : void 0;
@@ -1512,7 +1523,7 @@ async function prepareTools({
         anthropicTools2.push({
           name: tool.name,
           description: tool.description,
-          input_schema: tool.inputSchema,
+          input_schema: clewdr ? sanitizeJsonSchema(tool.inputSchema) : tool.inputSchema,
           cache_control: cacheControl,
           ...eagerInputStreaming ? { eager_input_streaming: true } : {},
           ...supportsStrictTools === true && tool.strict != null ? { strict: tool.strict } : {},
@@ -2261,7 +2272,8 @@ async function convertToAnthropicMessagesPrompt({
   sendReasoning,
   warnings,
   cacheControlValidator,
-  toolNameMapping
+  toolNameMapping,
+  clewdr
 }) {
   var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u;
   const betas = /* @__PURE__ */ new Set();
@@ -2400,9 +2412,26 @@ async function convertToAnthropicMessagesPrompt({
                         cache_control: cacheControl
                       });
                     } else {
-                      throw new UnsupportedFunctionalityError2({
-                        functionality: `media type: ${part.mediaType}`
-                      });
+                      const metadata = await getDocumentMetadata(
+                        part.providerOptions
+                      );
+                      if (clewdr) {
+                        anthropicContent.push({
+                          type: "document",
+                          source: {
+                            type: "base64",
+                            media_type: part.mediaType,
+                            data: convertToBase64(part.data)
+                          },
+                          title: metadata.title != null ? metadata.title : part.filename != null ? part.filename : "file",
+                          ...metadata.context && { context: metadata.context },
+                          cache_control: cacheControl
+                        });
+                      } else {
+                        throw new UnsupportedFunctionalityError2({
+                          functionality: `media type: ${part.mediaType}`
+                        });
+                      }
                     }
                     break;
                   }
@@ -3505,23 +3534,25 @@ var AnthropicMessagesLanguageModel = class {
         "anthropic.advisor_20260301": "advisor"
       }
     });
+    const clewdr = this.config.provider === "clewdr";
     const { prompt: messagesPrompt, betas } = await convertToAnthropicMessagesPrompt({
       prompt,
       sendReasoning: (_d = anthropicOptions == null ? void 0 : anthropicOptions.sendReasoning) != null ? _d : true,
       warnings,
       cacheControlValidator,
-      toolNameMapping
+      toolNameMapping,
+      clewdr
     });
     const thinkingType = (_e = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _e.type;
     const isThinking = thinkingType === "enabled" || thinkingType === "adaptive";
     let thinkingBudget = thinkingType === "enabled" ? (_f = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _f.budgetTokens : void 0;
     const thinkingDisplay = thinkingType === "adaptive" ? (_g = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _g.display : void 0;
-    const maxTokens = maxOutputTokens != null ? maxOutputTokens : maxOutputTokensForModel;
+    const maxTokens = maxOutputTokens != null ? maxOutputTokens : !clewdr || isKnownModel || this.modelId.startsWith("claude-") ? maxOutputTokensForModel : void 0;
     const baseArgs = {
       // model id:
       model: this.modelId,
       // standardized settings:
-      max_tokens: maxTokens,
+      ...clewdr && maxTokens == null ? {} : { max_tokens: maxTokens },
       temperature,
       top_k: topK,
       top_p: topP,
@@ -3534,6 +3565,9 @@ var AnthropicMessagesLanguageModel = class {
           ...thinkingDisplay != null && { display: thinkingDisplay }
         }
       },
+      ...clewdr && thinkingType === "disabled" && {
+        thinking: { type: "disabled" }
+      },
       ...((anthropicOptions == null ? void 0 : anthropicOptions.effort) || (anthropicOptions == null ? void 0 : anthropicOptions.taskBudget) || useStructuredOutput && (responseFormat == null ? void 0 : responseFormat.type) === "json" && responseFormat.schema != null) && {
         output_config: {
           ...(anthropicOptions == null ? void 0 : anthropicOptions.effort) && {
@@ -3692,7 +3726,9 @@ var AnthropicMessagesLanguageModel = class {
           details: "topP is not supported when thinking is enabled"
         });
       }
-      baseArgs.max_tokens = maxTokens + (thinkingBudget != null ? thinkingBudget : 0);
+      if (!clewdr || maxTokens != null) {
+        baseArgs.max_tokens = maxTokens + (thinkingBudget != null ? thinkingBudget : 0);
+      }
     } else {
       if (isAnthropicModel && topP != null && temperature != null) {
         warnings.push({
@@ -3758,7 +3794,8 @@ var AnthropicMessagesLanguageModel = class {
         cacheControlValidator,
         supportsStructuredOutput: false,
         supportsStrictTools,
-        defaultEagerInputStreaming
+        defaultEagerInputStreaming,
+        clewdr
       } : {
         tools: tools != null ? tools : [],
         toolChoice,
@@ -3766,7 +3803,8 @@ var AnthropicMessagesLanguageModel = class {
         cacheControlValidator,
         supportsStructuredOutput,
         supportsStrictTools,
-        defaultEagerInputStreaming
+        defaultEagerInputStreaming,
+        clewdr
       }
     );
     const cacheWarnings = cacheControlValidator.getWarnings();
@@ -4393,6 +4431,7 @@ var AnthropicMessagesLanguageModel = class {
     let isJsonResponseFromTool = false;
     let blockType = void 0;
     const generateId3 = this.generateId;
+    const clewdr = this.config.provider === "clewdr";
     const transformedStream = response.pipeThrough(
       new TransformStream({
         start(controller) {
@@ -4404,6 +4443,10 @@ var AnthropicMessagesLanguageModel = class {
             controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
           }
           if (!chunk.success) {
+            const rawType = typeof chunk.rawValue === "object" && chunk.rawValue != null ? chunk.rawValue.type : void 0;
+            if (clewdr && typeof rawType === "string" && !anthropicMessagesChunkTypes.has(rawType)) {
+              return;
+            }
             controller.enqueue({ type: "error", error: chunk.error });
             return;
           }
diff --git a/dist/internal/index.js b/dist/internal/index.js
index af85cb12ca16878f93dae60e5a590124c2627f10..ebcb76759419d2294a98756daf0f361b9899eb7d 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -852,6 +852,16 @@ var anthropicMessagesChunkSchema = (0, import_provider_utils2.lazySchema)(
     ])
   )
 );
+var anthropicMessagesChunkTypes = /* @__PURE__ */ new Set([
+  "message_start",
+  "content_block_start",
+  "content_block_delta",
+  "content_block_stop",
+  "message_delta",
+  "message_stop",
+  "ping",
+  "error"
+]);
 var anthropicReasoningMetadataSchema = (0, import_provider_utils2.lazySchema)(
   () => (0, import_provider_utils2.zodSchema)(
     import_v42.z.object({
@@ -1448,7 +1458,8 @@ async function prepareTools({
   cacheControlValidator,
   supportsStructuredOutput,
   supportsStrictTools,
-  defaultEagerInputStreaming = false
+  defaultEagerInputStreaming = false,
+  clewdr
 }) {
   var _a, _b;
   tools = (tools == null ? void 0 : tools.length) ? tools : void 0;
@@ -1480,7 +1491,7 @@ async function prepareTools({
         anthropicTools2.push({
           name: tool.name,
           description: tool.description,
-          input_schema: tool.inputSchema,
+          input_schema: clewdr ? sanitizeJsonSchema(tool.inputSchema) : tool.inputSchema,
           cache_control: cacheControl,
           ...eagerInputStreaming ? { eager_input_streaming: true } : {},
           ...supportsStrictTools === true && tool.strict != null ? { strict: tool.strict } : {},
@@ -2204,7 +2215,8 @@ async function convertToAnthropicMessagesPrompt({
   sendReasoning,
   warnings,
   cacheControlValidator,
-  toolNameMapping
+  toolNameMapping,
+  clewdr
 }) {
   var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u;
   const betas = /* @__PURE__ */ new Set();
@@ -2343,9 +2355,26 @@ async function convertToAnthropicMessagesPrompt({
                         cache_control: cacheControl
                       });
                     } else {
-                      throw new import_provider2.UnsupportedFunctionalityError({
-                        functionality: `media type: ${part.mediaType}`
-                      });
+                      const metadata = await getDocumentMetadata(
+                        part.providerOptions
+                      );
+                      if (clewdr) {
+                        anthropicContent.push({
+                          type: "document",
+                          source: {
+                            type: "base64",
+                            media_type: part.mediaType,
+                            data: (0, import_provider_utils2.convertToBase64)(part.data)
+                          },
+                          title: metadata.title != null ? metadata.title : part.filename != null ? part.filename : "file",
+                          ...metadata.context && { context: metadata.context },
+                          cache_control: cacheControl
+                        });
+                      } else {
+                        throw new import_provider2.UnsupportedFunctionalityError({
+                          functionality: `media type: ${part.mediaType}`
+                        });
+                      }
                     }
                     break;
                   }
@@ -3448,23 +3477,25 @@ var AnthropicMessagesLanguageModel = class {
         "anthropic.advisor_20260301": "advisor"
       }
     });
+    const clewdr = this.config.provider === "clewdr";
     const { prompt: messagesPrompt, betas } = await convertToAnthropicMessagesPrompt({
       prompt,
       sendReasoning: (_d = anthropicOptions == null ? void 0 : anthropicOptions.sendReasoning) != null ? _d : true,
       warnings,
       cacheControlValidator,
-      toolNameMapping
+      toolNameMapping,
+      clewdr
     });
     const thinkingType = (_e = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _e.type;
     const isThinking = thinkingType === "enabled" || thinkingType === "adaptive";
     let thinkingBudget = thinkingType === "enabled" ? (_f = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _f.budgetTokens : void 0;
     const thinkingDisplay = thinkingType === "adaptive" ? (_g = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _g.display : void 0;
-    const maxTokens = maxOutputTokens != null ? maxOutputTokens : maxOutputTokensForModel;
+    const maxTokens = maxOutputTokens != null ? maxOutputTokens : !clewdr || isKnownModel || this.modelId.startsWith("claude-") ? maxOutputTokensForModel : void 0;
     const baseArgs = {
       // model id:
       model: this.modelId,
       // standardized settings:
-      max_tokens: maxTokens,
+      ...clewdr && maxTokens == null ? {} : { max_tokens: maxTokens },
       temperature,
       top_k: topK,
       top_p: topP,
@@ -3477,6 +3508,9 @@ var AnthropicMessagesLanguageModel = class {
           ...thinkingDisplay != null && { display: thinkingDisplay }
         }
       },
+      ...clewdr && thinkingType === "disabled" && {
+        thinking: { type: "disabled" }
+      },
       ...((anthropicOptions == null ? void 0 : anthropicOptions.effort) || (anthropicOptions == null ? void 0 : anthropicOptions.taskBudget) || useStructuredOutput && (responseFormat == null ? void 0 : responseFormat.type) === "json" && responseFormat.schema != null) && {
         output_config: {
           ...(anthropicOptions == null ? void 0 : anthropicOptions.effort) && {
@@ -3635,7 +3669,9 @@ var AnthropicMessagesLanguageModel = class {
           details: "topP is not supported when thinking is enabled"
         });
       }
-      baseArgs.max_tokens = maxTokens + (thinkingBudget != null ? thinkingBudget : 0);
+      if (!clewdr || maxTokens != null) {
+        baseArgs.max_tokens = maxTokens + (thinkingBudget != null ? thinkingBudget : 0);
+      }
     } else {
       if (isAnthropicModel && topP != null && temperature != null) {
         warnings.push({
@@ -3701,7 +3737,8 @@ var AnthropicMessagesLanguageModel = class {
         cacheControlValidator,
         supportsStructuredOutput: false,
         supportsStrictTools,
-        defaultEagerInputStreaming
+        defaultEagerInputStreaming,
+        clewdr
       } : {
         tools: tools != null ? tools : [],
         toolChoice,
@@ -3709,7 +3746,8 @@ var AnthropicMessagesLanguageModel = class {
         cacheControlValidator,
         supportsStructuredOutput,
         supportsStrictTools,
-        defaultEagerInputStreaming
+        defaultEagerInputStreaming,
+        clewdr
       }
     );
     const cacheWarnings = cacheControlValidator.getWarnings();
@@ -4336,6 +4374,7 @@ var AnthropicMessagesLanguageModel = class {
     let isJsonResponseFromTool = false;
     let blockType = void 0;
     const generateId2 = this.generateId;
+    const clewdr = this.config.provider === "clewdr";
     const transformedStream = response.pipeThrough(
       new TransformStream({
         start(controller) {
@@ -4347,6 +4386,10 @@ var AnthropicMessagesLanguageModel = class {
             controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
           }
           if (!chunk.success) {
+            const rawType = typeof chunk.rawValue === "object" && chunk.rawValue != null ? chunk.rawValue.type : void 0;
+            if (clewdr && typeof rawType === "string" && !anthropicMessagesChunkTypes.has(rawType)) {
+              return;
+            }
             controller.enqueue({ type: "error", error: chunk.error });
             return;
           }
diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
index 9c37d988a10eb2f3c7b18094166eb11ccccbe152..26b8aa2afacd14f840ef319d2780de0826a91c33 100644
--- a/dist/internal/index.mjs
+++ b/dist/internal/index.mjs
@@ -842,6 +842,16 @@ var anthropicMessagesChunkSchema = lazySchema2(
     ])
   )
 );
+var anthropicMessagesChunkTypes = /* @__PURE__ */ new Set([
+  "message_start",
+  "content_block_start",
+  "content_block_delta",
+  "content_block_stop",
+  "message_delta",
+  "message_stop",
+  "ping",
+  "error"
+]);
 var anthropicReasoningMetadataSchema = lazySchema2(
   () => zodSchema2(
     z2.object({
@@ -1464,7 +1474,8 @@ async function prepareTools({
   cacheControlValidator,
   supportsStructuredOutput,
   supportsStrictTools,
-  defaultEagerInputStreaming = false
+  defaultEagerInputStreaming = false,
+  clewdr
 }) {
   var _a, _b;
   tools = (tools == null ? void 0 : tools.length) ? tools : void 0;
@@ -1496,7 +1507,7 @@ async function prepareTools({
         anthropicTools2.push({
           name: tool.name,
           description: tool.description,
-          input_schema: tool.inputSchema,
+          input_schema: clewdr ? sanitizeJsonSchema(tool.inputSchema) : tool.inputSchema,
           cache_control: cacheControl,
           ...eagerInputStreaming ? { eager_input_streaming: true } : {},
           ...supportsStrictTools === true && tool.strict != null ? { strict: tool.strict } : {},
@@ -2245,7 +2256,8 @@ async function convertToAnthropicMessagesPrompt({
   sendReasoning,
   warnings,
   cacheControlValidator,
-  toolNameMapping
+  toolNameMapping,
+  clewdr
 }) {
   var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u;
   const betas = /* @__PURE__ */ new Set();
@@ -2384,9 +2396,26 @@ async function convertToAnthropicMessagesPrompt({
                         cache_control: cacheControl
                       });
                     } else {
-                      throw new UnsupportedFunctionalityError2({
-                        functionality: `media type: ${part.mediaType}`
-                      });
+                      const metadata = await getDocumentMetadata(
+                        part.providerOptions
+                      );
+                      if (clewdr) {
+                        anthropicContent.push({
+                          type: "document",
+                          source: {
+                            type: "base64",
+                            media_type: part.mediaType,
+                            data: convertToBase64(part.data)
+                          },
+                          title: metadata.title != null ? metadata.title : part.filename != null ? part.filename : "file",
+                          ...metadata.context && { context: metadata.context },
+                          cache_control: cacheControl
+                        });
+                      } else {
+                        throw new UnsupportedFunctionalityError2({
+                          functionality: `media type: ${part.mediaType}`
+                        });
+                      }
                     }
                     break;
                   }
@@ -3489,23 +3518,25 @@ var AnthropicMessagesLanguageModel = class {
         "anthropic.advisor_20260301": "advisor"
       }
     });
+    const clewdr = this.config.provider === "clewdr";
     const { prompt: messagesPrompt, betas } = await convertToAnthropicMessagesPrompt({
       prompt,
       sendReasoning: (_d = anthropicOptions == null ? void 0 : anthropicOptions.sendReasoning) != null ? _d : true,
       warnings,
       cacheControlValidator,
-      toolNameMapping
+      toolNameMapping,
+      clewdr
     });
     const thinkingType = (_e = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _e.type;
     const isThinking = thinkingType === "enabled" || thinkingType === "adaptive";
     let thinkingBudget = thinkingType === "enabled" ? (_f = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _f.budgetTokens : void 0;
     const thinkingDisplay = thinkingType === "adaptive" ? (_g = anthropicOptions == null ? void 0 : anthropicOptions.thinking) == null ? void 0 : _g.display : void 0;
-    const maxTokens = maxOutputTokens != null ? maxOutputTokens : maxOutputTokensForModel;
+    const maxTokens = maxOutputTokens != null ? maxOutputTokens : !clewdr || isKnownModel || this.modelId.startsWith("claude-") ? maxOutputTokensForModel : void 0;
     const baseArgs = {
       // model id:
       model: this.modelId,
       // standardized settings:
-      max_tokens: maxTokens,
+      ...clewdr && maxTokens == null ? {} : { max_tokens: maxTokens },
       temperature,
       top_k: topK,
       top_p: topP,
@@ -3518,6 +3549,9 @@ var AnthropicMessagesLanguageModel = class {
           ...thinkingDisplay != null && { display: thinkingDisplay }
         }
       },
+      ...clewdr && thinkingType === "disabled" && {
+        thinking: { type: "disabled" }
+      },
       ...((anthropicOptions == null ? void 0 : anthropicOptions.effort) || (anthropicOptions == null ? void 0 : anthropicOptions.taskBudget) || useStructuredOutput && (responseFormat == null ? void 0 : responseFormat.type) === "json" && responseFormat.schema != null) && {
         output_config: {
           ...(anthropicOptions == null ? void 0 : anthropicOptions.effort) && {
@@ -3676,7 +3710,9 @@ var AnthropicMessagesLanguageModel = class {
           details: "topP is not supported when thinking is enabled"
         });
       }
-      baseArgs.max_tokens = maxTokens + (thinkingBudget != null ? thinkingBudget : 0);
+      if (!clewdr || maxTokens != null) {
+        baseArgs.max_tokens = maxTokens + (thinkingBudget != null ? thinkingBudget : 0);
+      }
     } else {
       if (isAnthropicModel && topP != null && temperature != null) {
         warnings.push({
@@ -3742,7 +3778,8 @@ var AnthropicMessagesLanguageModel = class {
         cacheControlValidator,
         supportsStructuredOutput: false,
         supportsStrictTools,
-        defaultEagerInputStreaming
+        defaultEagerInputStreaming,
+        clewdr
       } : {
         tools: tools != null ? tools : [],
         toolChoice,
@@ -3750,7 +3787,8 @@ var AnthropicMessagesLanguageModel = class {
         cacheControlValidator,
         supportsStructuredOutput,
         supportsStrictTools,
-        defaultEagerInputStreaming
+        defaultEagerInputStreaming,
+        clewdr
       }
     );
     const cacheWarnings = cacheControlValidator.getWarnings();
@@ -4377,6 +4415,7 @@ var AnthropicMessagesLanguageModel = class {
     let isJsonResponseFromTool = false;
     let blockType = void 0;
     const generateId2 = this.generateId;
+    const clewdr = this.config.provider === "clewdr";
     const transformedStream = response.pipeThrough(
       new TransformStream({
         start(controller) {
@@ -4388,6 +4427,10 @@ var AnthropicMessagesLanguageModel = class {
             controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
           }
           if (!chunk.success) {
+            const rawType = typeof chunk.rawValue === "object" && chunk.rawValue != null ? chunk.rawValue.type : void 0;
+            if (clewdr && typeof rawType === "string" && !anthropicMessagesChunkTypes.has(rawType)) {
+              return;
+            }
             controller.enqueue({ type: "error", error: chunk.error });
             return;
           }
