From 19a9cfe590ccd72ea28854457ba22d9079b32c0f Mon Sep 17 00:00:00 2001
From: cherry-studio-patch automation <actions@users.noreply.github.com>
Date: Wed, 15 Jul 2026 18:50:40 +0000
Subject: [PATCH] fix(clewdr): isolate remote tool activity

Signed-off-by: cherry-studio-patch automation <actions@users.noreply.github.com>
---
 patches/@ai-sdk__anthropic.patch              | 436 ++++++++++++++++--
 pnpm-lock.yaml                                |  16 +-
 .../__tests__/clewdrCompatibility.test.ts     |  78 +++-
 3 files changed, 476 insertions(+), 54 deletions(-)

diff --git a/patches/@ai-sdk__anthropic.patch b/patches/@ai-sdk__anthropic.patch
index 887e473..f1d43f5 100644
--- a/patches/@ai-sdk__anthropic.patch
+++ b/patches/@ai-sdk__anthropic.patch
@@ -1,5 +1,5 @@
 diff --git a/dist/index.js b/dist/index.js
-index 55e5a95..fd1fa9b 100644
+index 55e5a95..a181534 100644
 --- a/dist/index.js
 +++ b/dist/index.js
 @@ -684,6 +684,11 @@ var anthropicMessagesChunkSchema = (0, import_provider_utils2.lazySchema)(
@@ -105,7 +105,58 @@ index 55e5a95..fd1fa9b 100644
      } else {
        if (isAnthropicModel && topP != null && temperature != null) {
          warnings.push({
-@@ -3855,6 +3886,7 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3285,16 +3316,16 @@ var AnthropicMessagesLanguageModel = class {
+       betas: toolsBetas
+     } = await prepareTools(
+       jsonResponseTool != null ? {
+-        tools: [...tools != null ? tools : [], jsonResponseTool],
+-        toolChoice: { type: "required" },
++        tools: clewdr ? [] : [...tools != null ? tools : [], jsonResponseTool],
++        toolChoice: clewdr ? void 0 : { type: "required" },
+         disableParallelToolUse: true,
+         cacheControlValidator,
+         supportsStructuredOutput: false,
+         supportsStrictTools,
+         defaultEagerInputStreaming
+       } : {
+-        tools: tools != null ? tools : [],
+-        toolChoice,
++        tools: clewdr ? [] : tools != null ? tools : [],
++        toolChoice: clewdr ? void 0 : toolChoice,
+         disableParallelToolUse: anthropicOptions == null ? void 0 : anthropicOptions.disableParallelToolUse,
+         cacheControlValidator,
+         supportsStructuredOutput,
+@@ -3318,7 +3349,7 @@ var AnthropicMessagesLanguageModel = class {
+         ...userSuppliedBetas,
+         ...(_j = anthropicOptions == null ? void 0 : anthropicOptions.anthropicBeta) != null ? _j : []
+       ]),
+-      usesJsonResponseTool: jsonResponseTool != null,
++      usesJsonResponseTool: !clewdr && jsonResponseTool != null,
+       toolNameMapping,
+       providerOptionsName,
+       usedCustomProviderKey
+@@ -3417,7 +3448,11 @@ var AnthropicMessagesLanguageModel = class {
+     const mcpToolCalls = {};
+     const serverToolCalls = {};
+     let isJsonResponseFromTool = false;
++    const clewdr = this.config.provider === "clewdr";
+     for (const part of response.content) {
++      if (clewdr && part.type.includes("tool")) {
++        continue;
++      }
+       switch (part.type) {
+         case "text": {
+           if (!usesJsonResponseTool) {
+@@ -3753,7 +3788,7 @@ var AnthropicMessagesLanguageModel = class {
+       finishReason: {
+         unified: mapAnthropicStopReason({
+           finishReason: response.stop_reason,
+-          isJsonResponseFromTool
++          isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+         }),
+         raw: (_e = response.stop_reason) != null ? _e : void 0
+       },
+@@ -3855,6 +3890,7 @@ var AnthropicMessagesLanguageModel = class {
      let isJsonResponseFromTool = false;
      let blockType = void 0;
      const generateId3 = this.generateId;
@@ -113,20 +164,32 @@ index 55e5a95..fd1fa9b 100644
      const transformedStream = response.pipeThrough(
        new TransformStream({
          start(controller) {
-@@ -3866,6 +3898,12 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3862,10 +3898,13 @@ var AnthropicMessagesLanguageModel = class {
+         },
+         transform(chunk, controller) {
+           var _a2, _b2, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
+-          if (options.includeRawChunks) {
++          if (options.includeRawChunks || clewdr) {
              controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
            }
            if (!chunk.success) {
 +            if (clewdr) {
-+              if (!options.includeRawChunks) {
-+                controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
-+              }
 +              return;
 +            }
              controller.enqueue({ type: "error", error: chunk.error });
              return;
            }
-@@ -4369,6 +4407,15 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3877,6 +3916,9 @@ var AnthropicMessagesLanguageModel = class {
+             case "content_block_start": {
+               const part = value.content_block;
+               const contentBlockType = part.type;
++              if (clewdr && contentBlockType.includes("tool")) {
++                return;
++              }
+               blockType = contentBlockType;
+               switch (contentBlockType) {
+                 case "text": {
+@@ -4369,6 +4411,12 @@ var AnthropicMessagesLanguageModel = class {
                    }
                    return;
                  }
@@ -134,15 +197,39 @@ index 55e5a95..fd1fa9b 100644
 +                  if (!clewdr) {
 +                    throw new Error(`Unsupported delta type: ${deltaType}`);
 +                  }
-+                  if (!options.includeRawChunks) {
-+                    controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
-+                  }
 +                  return;
 +                }
                  case "citations_delta": {
                    const citation = value.delta.citation;
                    const source = createCitationSource(
-@@ -5268,4 +5315,4 @@ function forwardAnthropicContainerIdFromLastStep({
+@@ -4408,7 +4456,7 @@ var AnthropicMessagesLanguageModel = class {
+                 finishReason = {
+                   unified: mapAnthropicStopReason({
+                     finishReason: value.message.stop_reason,
+-                    isJsonResponseFromTool
++                    isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+                   }),
+                   raw: value.message.stop_reason
+                 };
+@@ -4421,7 +4469,7 @@ var AnthropicMessagesLanguageModel = class {
+               if (value.message.content != null) {
+                 for (let contentIndex = 0; contentIndex < value.message.content.length; contentIndex++) {
+                   const part = value.message.content[contentIndex];
+-                  if (part.type === "tool_use") {
++                  if (!clewdr && part.type === "tool_use") {
+                     const caller = part.caller;
+                     const callerInfo = caller ? {
+                       type: caller.type,
+@@ -4478,7 +4526,7 @@ var AnthropicMessagesLanguageModel = class {
+               finishReason = {
+                 unified: mapAnthropicStopReason({
+                   finishReason: value.delta.stop_reason,
+-                  isJsonResponseFromTool
++                  isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+                 }),
+                 raw: (_k = value.delta.stop_reason) != null ? _k : void 0
+               };
+@@ -5268,4 +5316,4 @@ function forwardAnthropicContainerIdFromLastStep({
    createAnthropic,
    forwardAnthropicContainerIdFromLastStep
  });
@@ -150,7 +237,7 @@ index 55e5a95..fd1fa9b 100644
 \ No newline at end of file
 +//# sourceMappingURL=index.js.map
 diff --git a/dist/index.mjs b/dist/index.mjs
-index f3f7cd2..0fb2d00 100644
+index f3f7cd2..57ab7aa 100644
 --- a/dist/index.mjs
 +++ b/dist/index.mjs
 @@ -679,6 +679,11 @@ var anthropicMessagesChunkSchema = lazySchema2(
@@ -256,7 +343,58 @@ index f3f7cd2..0fb2d00 100644
      } else {
        if (isAnthropicModel && topP != null && temperature != null) {
          warnings.push({
-@@ -3892,6 +3923,7 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3322,16 +3353,16 @@ var AnthropicMessagesLanguageModel = class {
+       betas: toolsBetas
+     } = await prepareTools(
+       jsonResponseTool != null ? {
+-        tools: [...tools != null ? tools : [], jsonResponseTool],
+-        toolChoice: { type: "required" },
++        tools: clewdr ? [] : [...tools != null ? tools : [], jsonResponseTool],
++        toolChoice: clewdr ? void 0 : { type: "required" },
+         disableParallelToolUse: true,
+         cacheControlValidator,
+         supportsStructuredOutput: false,
+         supportsStrictTools,
+         defaultEagerInputStreaming
+       } : {
+-        tools: tools != null ? tools : [],
+-        toolChoice,
++        tools: clewdr ? [] : tools != null ? tools : [],
++        toolChoice: clewdr ? void 0 : toolChoice,
+         disableParallelToolUse: anthropicOptions == null ? void 0 : anthropicOptions.disableParallelToolUse,
+         cacheControlValidator,
+         supportsStructuredOutput,
+@@ -3355,7 +3386,7 @@ var AnthropicMessagesLanguageModel = class {
+         ...userSuppliedBetas,
+         ...(_j = anthropicOptions == null ? void 0 : anthropicOptions.anthropicBeta) != null ? _j : []
+       ]),
+-      usesJsonResponseTool: jsonResponseTool != null,
++      usesJsonResponseTool: !clewdr && jsonResponseTool != null,
+       toolNameMapping,
+       providerOptionsName,
+       usedCustomProviderKey
+@@ -3454,7 +3485,11 @@ var AnthropicMessagesLanguageModel = class {
+     const mcpToolCalls = {};
+     const serverToolCalls = {};
+     let isJsonResponseFromTool = false;
++    const clewdr = this.config.provider === "clewdr";
+     for (const part of response.content) {
++      if (clewdr && part.type.includes("tool")) {
++        continue;
++      }
+       switch (part.type) {
+         case "text": {
+           if (!usesJsonResponseTool) {
+@@ -3790,7 +3825,7 @@ var AnthropicMessagesLanguageModel = class {
+       finishReason: {
+         unified: mapAnthropicStopReason({
+           finishReason: response.stop_reason,
+-          isJsonResponseFromTool
++          isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+         }),
+         raw: (_e = response.stop_reason) != null ? _e : void 0
+       },
+@@ -3892,6 +3927,7 @@ var AnthropicMessagesLanguageModel = class {
      let isJsonResponseFromTool = false;
      let blockType = void 0;
      const generateId3 = this.generateId;
@@ -264,20 +402,32 @@ index f3f7cd2..0fb2d00 100644
      const transformedStream = response.pipeThrough(
        new TransformStream({
          start(controller) {
-@@ -3903,6 +3935,12 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3899,10 +3935,13 @@ var AnthropicMessagesLanguageModel = class {
+         },
+         transform(chunk, controller) {
+           var _a2, _b2, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
+-          if (options.includeRawChunks) {
++          if (options.includeRawChunks || clewdr) {
              controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
            }
            if (!chunk.success) {
 +            if (clewdr) {
-+              if (!options.includeRawChunks) {
-+                controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
-+              }
 +              return;
 +            }
              controller.enqueue({ type: "error", error: chunk.error });
              return;
            }
-@@ -4406,6 +4444,15 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3914,6 +3953,9 @@ var AnthropicMessagesLanguageModel = class {
+             case "content_block_start": {
+               const part = value.content_block;
+               const contentBlockType = part.type;
++              if (clewdr && contentBlockType.includes("tool")) {
++                return;
++              }
+               blockType = contentBlockType;
+               switch (contentBlockType) {
+                 case "text": {
+@@ -4406,6 +4448,12 @@ var AnthropicMessagesLanguageModel = class {
                    }
                    return;
                  }
@@ -285,15 +435,39 @@ index f3f7cd2..0fb2d00 100644
 +                  if (!clewdr) {
 +                    throw new Error(`Unsupported delta type: ${deltaType}`);
 +                  }
-+                  if (!options.includeRawChunks) {
-+                    controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
-+                  }
 +                  return;
 +                }
                  case "citations_delta": {
                    const citation = value.delta.citation;
                    const source = createCitationSource(
-@@ -5344,4 +5391,4 @@ export {
+@@ -4445,7 +4493,7 @@ var AnthropicMessagesLanguageModel = class {
+                 finishReason = {
+                   unified: mapAnthropicStopReason({
+                     finishReason: value.message.stop_reason,
+-                    isJsonResponseFromTool
++                    isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+                   }),
+                   raw: value.message.stop_reason
+                 };
+@@ -4458,7 +4506,7 @@ var AnthropicMessagesLanguageModel = class {
+               if (value.message.content != null) {
+                 for (let contentIndex = 0; contentIndex < value.message.content.length; contentIndex++) {
+                   const part = value.message.content[contentIndex];
+-                  if (part.type === "tool_use") {
++                  if (!clewdr && part.type === "tool_use") {
+                     const caller = part.caller;
+                     const callerInfo = caller ? {
+                       type: caller.type,
+@@ -4515,7 +4563,7 @@ var AnthropicMessagesLanguageModel = class {
+               finishReason = {
+                 unified: mapAnthropicStopReason({
+                   finishReason: value.delta.stop_reason,
+-                  isJsonResponseFromTool
++                  isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+                 }),
+                 raw: (_k = value.delta.stop_reason) != null ? _k : void 0
+               };
+@@ -5344,4 +5392,4 @@ export {
    createAnthropic,
    forwardAnthropicContainerIdFromLastStep
  };
@@ -301,7 +475,7 @@ index f3f7cd2..0fb2d00 100644
 \ No newline at end of file
 +//# sourceMappingURL=index.mjs.map
 diff --git a/dist/internal/index.js b/dist/internal/index.js
-index 468e751..ef78ae3 100644
+index 468e751..8cfedf3 100644
 --- a/dist/internal/index.js
 +++ b/dist/internal/index.js
 @@ -676,6 +676,11 @@ var anthropicMessagesChunkSchema = (0, import_provider_utils2.lazySchema)(
@@ -407,7 +581,58 @@ index 468e751..ef78ae3 100644
      } else {
        if (isAnthropicModel && topP != null && temperature != null) {
          warnings.push({
-@@ -3847,6 +3878,7 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3277,16 +3308,16 @@ var AnthropicMessagesLanguageModel = class {
+       betas: toolsBetas
+     } = await prepareTools(
+       jsonResponseTool != null ? {
+-        tools: [...tools != null ? tools : [], jsonResponseTool],
+-        toolChoice: { type: "required" },
++        tools: clewdr ? [] : [...tools != null ? tools : [], jsonResponseTool],
++        toolChoice: clewdr ? void 0 : { type: "required" },
+         disableParallelToolUse: true,
+         cacheControlValidator,
+         supportsStructuredOutput: false,
+         supportsStrictTools,
+         defaultEagerInputStreaming
+       } : {
+-        tools: tools != null ? tools : [],
+-        toolChoice,
++        tools: clewdr ? [] : tools != null ? tools : [],
++        toolChoice: clewdr ? void 0 : toolChoice,
+         disableParallelToolUse: anthropicOptions == null ? void 0 : anthropicOptions.disableParallelToolUse,
+         cacheControlValidator,
+         supportsStructuredOutput,
+@@ -3310,7 +3341,7 @@ var AnthropicMessagesLanguageModel = class {
+         ...userSuppliedBetas,
+         ...(_j = anthropicOptions == null ? void 0 : anthropicOptions.anthropicBeta) != null ? _j : []
+       ]),
+-      usesJsonResponseTool: jsonResponseTool != null,
++      usesJsonResponseTool: !clewdr && jsonResponseTool != null,
+       toolNameMapping,
+       providerOptionsName,
+       usedCustomProviderKey
+@@ -3409,7 +3440,11 @@ var AnthropicMessagesLanguageModel = class {
+     const mcpToolCalls = {};
+     const serverToolCalls = {};
+     let isJsonResponseFromTool = false;
++    const clewdr = this.config.provider === "clewdr";
+     for (const part of response.content) {
++      if (clewdr && part.type.includes("tool")) {
++        continue;
++      }
+       switch (part.type) {
+         case "text": {
+           if (!usesJsonResponseTool) {
+@@ -3745,7 +3780,7 @@ var AnthropicMessagesLanguageModel = class {
+       finishReason: {
+         unified: mapAnthropicStopReason({
+           finishReason: response.stop_reason,
+-          isJsonResponseFromTool
++          isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+         }),
+         raw: (_e = response.stop_reason) != null ? _e : void 0
+       },
+@@ -3847,6 +3882,7 @@ var AnthropicMessagesLanguageModel = class {
      let isJsonResponseFromTool = false;
      let blockType = void 0;
      const generateId2 = this.generateId;
@@ -415,20 +640,32 @@ index 468e751..ef78ae3 100644
      const transformedStream = response.pipeThrough(
        new TransformStream({
          start(controller) {
-@@ -3858,6 +3890,12 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3854,10 +3890,13 @@ var AnthropicMessagesLanguageModel = class {
+         },
+         transform(chunk, controller) {
+           var _a2, _b2, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
+-          if (options.includeRawChunks) {
++          if (options.includeRawChunks || clewdr) {
              controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
            }
            if (!chunk.success) {
 +            if (clewdr) {
-+              if (!options.includeRawChunks) {
-+                controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
-+              }
 +              return;
 +            }
              controller.enqueue({ type: "error", error: chunk.error });
              return;
            }
-@@ -4361,6 +4399,15 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3869,6 +3908,9 @@ var AnthropicMessagesLanguageModel = class {
+             case "content_block_start": {
+               const part = value.content_block;
+               const contentBlockType = part.type;
++              if (clewdr && contentBlockType.includes("tool")) {
++                return;
++              }
+               blockType = contentBlockType;
+               switch (contentBlockType) {
+                 case "text": {
+@@ -4361,6 +4403,12 @@ var AnthropicMessagesLanguageModel = class {
                    }
                    return;
                  }
@@ -436,15 +673,39 @@ index 468e751..ef78ae3 100644
 +                  if (!clewdr) {
 +                    throw new Error(`Unsupported delta type: ${deltaType}`);
 +                  }
-+                  if (!options.includeRawChunks) {
-+                    controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
-+                  }
 +                  return;
 +                }
                  case "citations_delta": {
                    const citation = value.delta.citation;
                    const source = createCitationSource(
-@@ -5168,4 +5215,4 @@ var anthropicTools = {
+@@ -4400,7 +4448,7 @@ var AnthropicMessagesLanguageModel = class {
+                 finishReason = {
+                   unified: mapAnthropicStopReason({
+                     finishReason: value.message.stop_reason,
+-                    isJsonResponseFromTool
++                    isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+                   }),
+                   raw: value.message.stop_reason
+                 };
+@@ -4413,7 +4461,7 @@ var AnthropicMessagesLanguageModel = class {
+               if (value.message.content != null) {
+                 for (let contentIndex = 0; contentIndex < value.message.content.length; contentIndex++) {
+                   const part = value.message.content[contentIndex];
+-                  if (part.type === "tool_use") {
++                  if (!clewdr && part.type === "tool_use") {
+                     const caller = part.caller;
+                     const callerInfo = caller ? {
+                       type: caller.type,
+@@ -4470,7 +4518,7 @@ var AnthropicMessagesLanguageModel = class {
+               finishReason = {
+                 unified: mapAnthropicStopReason({
+                   finishReason: value.delta.stop_reason,
+-                  isJsonResponseFromTool
++                  isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+                 }),
+                 raw: (_k = value.delta.stop_reason) != null ? _k : void 0
+               };
+@@ -5168,4 +5216,4 @@ var anthropicTools = {
    anthropicTools,
    prepareTools
  });
@@ -452,7 +713,7 @@ index 468e751..ef78ae3 100644
 \ No newline at end of file
 +//# sourceMappingURL=index.js.map
 diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
-index 6e9ff51..2046c9a 100644
+index 6e9ff51..52914e3 100644
 --- a/dist/internal/index.mjs
 +++ b/dist/internal/index.mjs
 @@ -663,6 +663,11 @@ var anthropicMessagesChunkSchema = lazySchema2(
@@ -558,7 +819,58 @@ index 6e9ff51..2046c9a 100644
      } else {
        if (isAnthropicModel && topP != null && temperature != null) {
          warnings.push({
-@@ -3876,6 +3907,7 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3306,16 +3337,16 @@ var AnthropicMessagesLanguageModel = class {
+       betas: toolsBetas
+     } = await prepareTools(
+       jsonResponseTool != null ? {
+-        tools: [...tools != null ? tools : [], jsonResponseTool],
+-        toolChoice: { type: "required" },
++        tools: clewdr ? [] : [...tools != null ? tools : [], jsonResponseTool],
++        toolChoice: clewdr ? void 0 : { type: "required" },
+         disableParallelToolUse: true,
+         cacheControlValidator,
+         supportsStructuredOutput: false,
+         supportsStrictTools,
+         defaultEagerInputStreaming
+       } : {
+-        tools: tools != null ? tools : [],
+-        toolChoice,
++        tools: clewdr ? [] : tools != null ? tools : [],
++        toolChoice: clewdr ? void 0 : toolChoice,
+         disableParallelToolUse: anthropicOptions == null ? void 0 : anthropicOptions.disableParallelToolUse,
+         cacheControlValidator,
+         supportsStructuredOutput,
+@@ -3339,7 +3370,7 @@ var AnthropicMessagesLanguageModel = class {
+         ...userSuppliedBetas,
+         ...(_j = anthropicOptions == null ? void 0 : anthropicOptions.anthropicBeta) != null ? _j : []
+       ]),
+-      usesJsonResponseTool: jsonResponseTool != null,
++      usesJsonResponseTool: !clewdr && jsonResponseTool != null,
+       toolNameMapping,
+       providerOptionsName,
+       usedCustomProviderKey
+@@ -3438,7 +3469,11 @@ var AnthropicMessagesLanguageModel = class {
+     const mcpToolCalls = {};
+     const serverToolCalls = {};
+     let isJsonResponseFromTool = false;
++    const clewdr = this.config.provider === "clewdr";
+     for (const part of response.content) {
++      if (clewdr && part.type.includes("tool")) {
++        continue;
++      }
+       switch (part.type) {
+         case "text": {
+           if (!usesJsonResponseTool) {
+@@ -3774,7 +3809,7 @@ var AnthropicMessagesLanguageModel = class {
+       finishReason: {
+         unified: mapAnthropicStopReason({
+           finishReason: response.stop_reason,
+-          isJsonResponseFromTool
++          isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+         }),
+         raw: (_e = response.stop_reason) != null ? _e : void 0
+       },
+@@ -3876,6 +3911,7 @@ var AnthropicMessagesLanguageModel = class {
      let isJsonResponseFromTool = false;
      let blockType = void 0;
      const generateId2 = this.generateId;
@@ -566,20 +878,32 @@ index 6e9ff51..2046c9a 100644
      const transformedStream = response.pipeThrough(
        new TransformStream({
          start(controller) {
-@@ -3887,6 +3919,12 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3883,10 +3919,13 @@ var AnthropicMessagesLanguageModel = class {
+         },
+         transform(chunk, controller) {
+           var _a2, _b2, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
+-          if (options.includeRawChunks) {
++          if (options.includeRawChunks || clewdr) {
              controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
            }
            if (!chunk.success) {
 +            if (clewdr) {
-+              if (!options.includeRawChunks) {
-+                controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
-+              }
 +              return;
 +            }
              controller.enqueue({ type: "error", error: chunk.error });
              return;
            }
-@@ -4390,6 +4428,15 @@ var AnthropicMessagesLanguageModel = class {
+@@ -3898,6 +3937,9 @@ var AnthropicMessagesLanguageModel = class {
+             case "content_block_start": {
+               const part = value.content_block;
+               const contentBlockType = part.type;
++              if (clewdr && contentBlockType.includes("tool")) {
++                return;
++              }
+               blockType = contentBlockType;
+               switch (contentBlockType) {
+                 case "text": {
+@@ -4390,6 +4432,12 @@ var AnthropicMessagesLanguageModel = class {
                    }
                    return;
                  }
@@ -587,15 +911,39 @@ index 6e9ff51..2046c9a 100644
 +                  if (!clewdr) {
 +                    throw new Error(`Unsupported delta type: ${deltaType}`);
 +                  }
-+                  if (!options.includeRawChunks) {
-+                    controller.enqueue({ type: "raw", rawValue: chunk.rawValue });
-+                  }
 +                  return;
 +                }
                  case "citations_delta": {
                    const citation = value.delta.citation;
                    const source = createCitationSource(
-@@ -5236,4 +5283,4 @@ export {
+@@ -4429,7 +4477,7 @@ var AnthropicMessagesLanguageModel = class {
+                 finishReason = {
+                   unified: mapAnthropicStopReason({
+                     finishReason: value.message.stop_reason,
+-                    isJsonResponseFromTool
++                    isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+                   }),
+                   raw: value.message.stop_reason
+                 };
+@@ -4442,7 +4490,7 @@ var AnthropicMessagesLanguageModel = class {
+               if (value.message.content != null) {
+                 for (let contentIndex = 0; contentIndex < value.message.content.length; contentIndex++) {
+                   const part = value.message.content[contentIndex];
+-                  if (part.type === "tool_use") {
++                  if (!clewdr && part.type === "tool_use") {
+                     const caller = part.caller;
+                     const callerInfo = caller ? {
+                       type: caller.type,
+@@ -4499,7 +4547,7 @@ var AnthropicMessagesLanguageModel = class {
+               finishReason = {
+                 unified: mapAnthropicStopReason({
+                   finishReason: value.delta.stop_reason,
+-                  isJsonResponseFromTool
++                  isJsonResponseFromTool: clewdr || isJsonResponseFromTool
+                 }),
+                 raw: (_k = value.delta.stop_reason) != null ? _k : void 0
+               };
+@@ -5236,4 +5284,4 @@ export {
    anthropicTools,
    prepareTools
  };
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index 7b214b7..f563f7b 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -35,7 +35,7 @@ overrides:
 
 patchedDependencies:
   '@ai-sdk/anthropic':
-    hash: b663f8b347bc3207d9c60e4c6c69839f6bbe1c232e647a0ca06307dd7419204e
+    hash: 3acf42cf5a9a43aac626d7b2be13da02ea70042c20f22d2ad60c6fec46b4dcf6
     path: patches/@ai-sdk__anthropic.patch
   '@ai-sdk/deepseek@2.0.30':
     hash: db4c4c1e60c18e61952ec4432de7a401adf44420bdbfe4bb6f726a8958642790
@@ -189,7 +189,7 @@ importers:
         version: 4.0.96(zod@4.3.4)
       '@ai-sdk/anthropic':
         specifier: ^3.0.71
-        version: 3.0.71(patch_hash=b663f8b347bc3207d9c60e4c6c69839f6bbe1c232e647a0ca06307dd7419204e)(zod@4.3.4)
+        version: 3.0.71(patch_hash=3acf42cf5a9a43aac626d7b2be13da02ea70042c20f22d2ad60c6fec46b4dcf6)(zod@4.3.4)
       '@ai-sdk/azure':
         specifier: ^3.0.54
         version: 3.0.54(zod@4.3.4)
@@ -1303,7 +1303,7 @@ importers:
     dependencies:
       '@ai-sdk/anthropic':
         specifier: ^3.0.71
-        version: 3.0.71(patch_hash=b663f8b347bc3207d9c60e4c6c69839f6bbe1c232e647a0ca06307dd7419204e)(zod@4.3.6)
+        version: 3.0.71(patch_hash=3acf42cf5a9a43aac626d7b2be13da02ea70042c20f22d2ad60c6fec46b4dcf6)(zod@4.3.6)
       '@ai-sdk/google':
         specifier: ^3.0.64
         version: 3.0.64(patch_hash=3dbdd2fbbe5a44bc23a04b1195329dd8d78043447e925342ad5ed53c8c3b96ec)(zod@4.3.6)
@@ -1334,7 +1334,7 @@ importers:
     dependencies:
       '@ai-sdk/anthropic':
         specifier: ^3.0.71
-        version: 3.0.71(patch_hash=b663f8b347bc3207d9c60e4c6c69839f6bbe1c232e647a0ca06307dd7419204e)(zod@4.3.4)
+        version: 3.0.71(patch_hash=3acf42cf5a9a43aac626d7b2be13da02ea70042c20f22d2ad60c6fec46b4dcf6)(zod@4.3.4)
       '@ai-sdk/azure':
         specifier: ^3.0.54
         version: 3.0.54(zod@4.3.4)
@@ -12933,7 +12933,7 @@ snapshots:
 
   '@ai-sdk/amazon-bedrock@4.0.96(zod@4.3.4)':
     dependencies:
-      '@ai-sdk/anthropic': 3.0.71(patch_hash=b663f8b347bc3207d9c60e4c6c69839f6bbe1c232e647a0ca06307dd7419204e)(zod@4.3.4)
+      '@ai-sdk/anthropic': 3.0.71(patch_hash=3acf42cf5a9a43aac626d7b2be13da02ea70042c20f22d2ad60c6fec46b4dcf6)(zod@4.3.4)
       '@ai-sdk/provider': 3.0.8
       '@ai-sdk/provider-utils': 4.0.23(zod@4.3.4)
       '@smithy/eventstream-codec': 4.2.14
@@ -12941,13 +12941,13 @@ snapshots:
       aws4fetch: 1.0.20
       zod: 4.3.4
 
-  '@ai-sdk/anthropic@3.0.71(patch_hash=b663f8b347bc3207d9c60e4c6c69839f6bbe1c232e647a0ca06307dd7419204e)(zod@4.3.4)':
+  '@ai-sdk/anthropic@3.0.71(patch_hash=3acf42cf5a9a43aac626d7b2be13da02ea70042c20f22d2ad60c6fec46b4dcf6)(zod@4.3.4)':
     dependencies:
       '@ai-sdk/provider': 3.0.8
       '@ai-sdk/provider-utils': 4.0.23(zod@4.3.4)
       zod: 4.3.4
 
-  '@ai-sdk/anthropic@3.0.71(patch_hash=b663f8b347bc3207d9c60e4c6c69839f6bbe1c232e647a0ca06307dd7419204e)(zod@4.3.6)':
+  '@ai-sdk/anthropic@3.0.71(patch_hash=3acf42cf5a9a43aac626d7b2be13da02ea70042c20f22d2ad60c6fec46b4dcf6)(zod@4.3.6)':
     dependencies:
       '@ai-sdk/provider': 3.0.8
       '@ai-sdk/provider-utils': 4.0.23(zod@4.3.6)
@@ -12995,7 +12995,7 @@ snapshots:
 
   '@ai-sdk/google-vertex@4.0.112(zod@4.3.4)':
     dependencies:
-      '@ai-sdk/anthropic': 3.0.71(patch_hash=b663f8b347bc3207d9c60e4c6c69839f6bbe1c232e647a0ca06307dd7419204e)(zod@4.3.4)
+      '@ai-sdk/anthropic': 3.0.71(patch_hash=3acf42cf5a9a43aac626d7b2be13da02ea70042c20f22d2ad60c6fec46b4dcf6)(zod@4.3.4)
       '@ai-sdk/google': 3.0.64(patch_hash=3dbdd2fbbe5a44bc23a04b1195329dd8d78043447e925342ad5ed53c8c3b96ec)(zod@4.3.4)
       '@ai-sdk/openai-compatible': 2.0.41(zod@4.3.4)
       '@ai-sdk/provider': 3.0.8
diff --git a/src/renderer/src/aiCore/provider/__tests__/clewdrCompatibility.test.ts b/src/renderer/src/aiCore/provider/__tests__/clewdrCompatibility.test.ts
index fb35346..706a01e 100644
--- a/src/renderer/src/aiCore/provider/__tests__/clewdrCompatibility.test.ts
+++ b/src/renderer/src/aiCore/provider/__tests__/clewdrCompatibility.test.ts
@@ -1,7 +1,8 @@
 import { createAnthropic } from '@ai-sdk/anthropic'
 import type { LanguageModelV3CallOptions, LanguageModelV3StreamPart } from '@ai-sdk/provider'
-import { streamText } from 'ai'
-import { describe, expect, it } from 'vitest'
+import { stepCountIs, streamText, tool } from 'ai'
+import { describe, expect, it, vi } from 'vitest'
+import { z } from 'zod'
 
 import { createClewdr } from '../custom/clewdr-provider'
 
@@ -64,6 +65,29 @@ const unsupportedWebEvents = [
   { type: 'content_block_delta', index: 0, delta: { type: 'thinking_cut_off_delta', cut_off: true } }
 ]
 
+const remoteToolEvents = [
+  {
+    type: 'message_start',
+    message: { id: 'msg-1', model: 'claude-sonnet-4-6', role: 'assistant', usage: { input_tokens: 1 }, content: [] }
+  },
+  {
+    type: 'content_block_start',
+    index: 0,
+    content_block: { type: 'tool_use', id: 'toolu-1', name: 'view', input: {} }
+  },
+  { type: 'content_block_delta', index: 0, delta: { type: 'input_json_delta', partial_json: '{"path":"a.txt"}' } },
+  toolUseBlockUpdate,
+  { type: 'content_block_stop', index: 0 },
+  {
+    type: 'message_delta',
+    delta: { stop_reason: 'tool_use', stop_sequence: null },
+    usage: { output_tokens: 1 }
+  },
+  { type: 'message_stop' }
+]
+
+const remoteToolStream = remoteToolEvents.map((event) => `data: ${JSON.stringify(event)}\n\n`).join('')
+
 async function readStream(stream: ReadableStream<LanguageModelV3StreamPart>) {
   const chunks: LanguageModelV3StreamPart[] = []
   const reader = stream.getReader()
@@ -203,6 +227,56 @@ describe('Clewdr Anthropic compatibility', () => {
     expect(requestCount).toBe(1)
   })
 
+  it('does not execute Clewdr remote tools or start a second message request', async () => {
+    let requestCount = 0
+    const requestBodies: Record<string, unknown>[] = []
+    const execute = vi.fn(async () => 'local result')
+    const fetch = ((_url: RequestInfo | URL, init?: RequestInit) => {
+      requestCount += 1
+      requestBodies.push(JSON.parse(String(init?.body)) as Record<string, unknown>)
+      return Promise.resolve(
+        new Response(remoteToolStream, {
+          headers: { 'content-type': 'text/event-stream' },
+          status: 200
+        })
+      )
+    }) as typeof globalThis.fetch
+    const result = streamText({
+      model: createClewdr({ apiKey: 'test', fetch }).languageModel('claude-sonnet-4-6'),
+      prompt: 'hello',
+      tools: {
+        view: tool({ inputSchema: z.object({ path: z.string() }), execute })
+      },
+      stopWhen: stepCountIs(2),
+      maxRetries: 0
+    })
+
+    await result.consumeStream()
+
+    expect(execute).not.toHaveBeenCalled()
+    expect(requestCount).toBe(1)
+    expect(requestBodies[0]).not.toHaveProperty('tools')
+    expect(requestBodies[0]).not.toHaveProperty('tool_choice')
+  })
+
+  it('preserves Clewdr remote tool activity as raw data without tool-call parts', async () => {
+    const fetch = (() =>
+      Promise.resolve(
+        new Response(remoteToolStream, {
+          headers: { 'content-type': 'text/event-stream' },
+          status: 200
+        })
+      )) as typeof globalThis.fetch
+    const result = await createClewdr({ apiKey: 'test', fetch }).languageModel('claude-sonnet-4-6').doStream(textPrompt)
+    const chunks = await readStream(result.stream)
+
+    expect(chunks.filter((chunk) => chunk.type === 'raw').map((chunk) => chunk.rawValue)).toEqual(remoteToolEvents)
+    expect(chunks.filter((chunk) => chunk.type.startsWith('tool-'))).toEqual([])
+    expect(chunks).toContainEqual(
+      expect.objectContaining({ type: 'finish', finishReason: { unified: 'stop', raw: 'tool_use' } })
+    )
+  })
+
   it('rejects Clewdr-only tool progress updates for the official Anthropic provider', async () => {
     const fetch = (() =>
       Promise.resolve(
-- 
2.43.0

