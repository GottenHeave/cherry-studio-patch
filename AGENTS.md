# Repository Instructions

Treat `CherryHQ/cherry-studio` as read-only upstream data. Keep downstream changes as replayable patches in `patches/cur`. The only current downstream behavior change is that chat attachment selection, drag-and-drop, and clipboard attachment paths accept every file type. Do not relax file restrictions for knowledge, avatars, plugins, or other upload flows.
